# Discord Bot Token (Developer Portal → Bot → Token)
DISCORD_TOKEN=

# Application / Client ID (Developer Portal → General Information → Application ID)
DISCORD_CLIENT_ID=

# Optional: Guild ID for instant slash-command registration (recommended while developing)
# Leave empty to register commands globally (can take up to ~1 hour to appear)
DISCORD_GUILD_ID=
